//redux == giant object with lots of properties, nested properties
const defaultState  = {

};

const homePageReducer = (state = defaultState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default homePageReducer;